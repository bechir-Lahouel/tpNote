name: CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout du code
        uses: actions/checkout@v4  # 👈 Télécharge le code du repo

      - name: Construire l'image Docker
        run: docker build -t bechirlahouel/tpnote:${{ github.sha }} .

      - name: Connexion à Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Pousser l'image Docker
        run: |
          docker tag bechirlahouel/tpnote:${{ github.sha }} bechirlahouel/tpnote:latest
          docker push bechirlahouel/tpnote:latest

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout du code  # 👈 Télécharge le repo pour avoir deployment.yml et service.yml
        uses: actions/checkout@v4

      - name: Installer kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: 'latest'

      - name: Vérifier la présence des fichiers
        run: ls -l

      - name: Déployer sur Kubernetes
        run: |
          kubectl apply -f deployment.yml
          kubectl apply -f service.yml
        env:
          KUBECONFIG: ${{ secrets.KUBECONFIG }}
